package org.hotel.back.controller;

import org.hotel.back.domain.Hotel;
import org.hotel.back.domain.Review;
import org.hotel.back.repository.HotelRepository;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.Optional;
import java.util.UUID;

import static org.junit.jupiter.api.Assertions.*;
@SpringBootTest
class HotelControllerTest {

//    @Autowired
//    private HotelRepository hotelRepository;

    @Test
    void testInsertWithImages(){
        //Optional<Hotel> result=hotelRepository.findByIdWithImages(1L);
}
}